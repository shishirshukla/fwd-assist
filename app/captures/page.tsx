import { CapturesList } from "@/components/captures-list";

export default function CapturesPage() {
  return (
    <div className="h-full overflow-y-auto bg-background">
      <CapturesList />
    </div>
  );
}
